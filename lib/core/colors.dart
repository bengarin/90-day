import 'package:flutter/material.dart';

/// Design system colors — warm, calm, "coach" feel (light theme).
class AppColors {
  static const background = Color(0xFFF6F5F0);
  static const card = Color(0xFFFFFFFF);
  static const ink = Color(0xFF26241F);
  static const inkSecondary = Color(0xFF6B6A63);
  static const line = Color(0xFFEAE8E0);

  // Primary (teal) — checkboxes, progress bars.
  static const primary = Color(0xFF0F6E56);
  static const primarySoft = Color(0xFFE1F5EE);

  // Amber — streak.
  static const amber = Color(0xFFBA7517);
  static const amberSoft = Color(0xFFFAEEDA);

  // Coral — alerts / emphasis.
  static const coral = Color(0xFFD85A30);
}
